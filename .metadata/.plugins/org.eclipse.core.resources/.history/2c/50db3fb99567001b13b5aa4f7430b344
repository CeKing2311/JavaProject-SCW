server:
  port: 7001
  
spring:
  application:
    name: ceking-crowd-eureka
    
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}/${server.port}/eureka    